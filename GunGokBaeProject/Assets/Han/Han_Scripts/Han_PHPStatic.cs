using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Han_PHPStatic : MonoBehaviour
{
    public static string Url = "http://localhost"; // + "/name.php" 넣어서 사용

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
